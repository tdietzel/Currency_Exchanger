export default function calculateExchange(selectedExchangeTo, response) {
  const currencyAmount = document.querySelector("input#userCurrencyAmount").value;
  const conversion = response.conversion_rates;
  
  switch (selectedExchangeTo) {
  case 'AED': return currencyAmount * conversion.AED;
  case 'AFN': return currencyAmount * conversion.AFN;
  case 'ALL': return currencyAmount * conversion.ALL;
  case 'AMD': return currencyAmount * conversion.AMD;
  case 'ANG': return currencyAmount * conversion.ANG;
  case 'AOA': return currencyAmount * conversion.AOA;
  case 'ARS': return currencyAmount * conversion.ARS;
  case 'AUD': return currencyAmount * conversion.AUD;
  case 'AWG': return currencyAmount * conversion.AWG;
  case 'AZN': return currencyAmount * conversion.AZN;
  case 'BAM': return currencyAmount * conversion.BAM;
  case 'BBD': return currencyAmount * conversion.BBD;
  case 'BDT': return currencyAmount * conversion.BDT;
  case 'BGN': return currencyAmount * conversion.BGN;
  case 'BHD': return currencyAmount * conversion.BHD;
  case 'BIF': return currencyAmount * conversion.BIF;
  case 'BMD': return currencyAmount * conversion.BMD;
  case 'BND': return currencyAmount * conversion.BND;
  case 'BOB': return currencyAmount * conversion.BOB;
  case 'BRL': return currencyAmount * conversion.BRL;
  case 'BSD': return currencyAmount * conversion.BSD;
  case 'BTN': return currencyAmount * conversion.BTN;
  case 'BWP': return currencyAmount * conversion.BWP;
  case 'BYN': return currencyAmount * conversion.BYN;
  case 'BZD': return currencyAmount * conversion.BZD;
  case 'CAD': return currencyAmount * conversion.CAD;
  case 'CDF': return currencyAmount * conversion.CDF;
  case 'CHF': return currencyAmount * conversion.CHF;
  case 'CLP': return currencyAmount * conversion.CLP;
  case 'CNY': return currencyAmount * conversion.CNY;
  case 'COP': return currencyAmount * conversion.COP;
  case 'CRC': return currencyAmount * conversion.CRC;
  case 'CUP': return currencyAmount * conversion.CUP;
  case 'CVE': return currencyAmount * conversion.CVE;
  case 'CZK': return currencyAmount * conversion.CZK;
  case 'DJF': return currencyAmount * conversion.DJF;
  case 'DKK': return currencyAmount * conversion.DKK;
  case 'DOP': return currencyAmount * conversion.DOP;
  case 'DZD': return currencyAmount * conversion.DZD;
  case 'EGP': return currencyAmount * conversion.EGP;
  case 'ERN': return currencyAmount * conversion.ERN;
  case 'ETB': return currencyAmount * conversion.ETB;
  case 'EUR': return currencyAmount * conversion.EUR;
  case 'FJD': return currencyAmount * conversion.FJD;
  case 'FKP': return currencyAmount * conversion.FKP;
  case 'FOK': return currencyAmount * conversion.FOK;
  case 'GBP': return currencyAmount * conversion.GBP;
  case 'GEL': return currencyAmount * conversion.GEL;
  case 'GGP': return currencyAmount * conversion.GGP;
  case 'GHS': return currencyAmount * conversion.GHS;
  case 'GIP': return currencyAmount * conversion.GIP;
  case 'GMD': return currencyAmount * conversion.GMD;
  case 'GNF': return currencyAmount * conversion.GNF;
  case 'GTQ': return currencyAmount * conversion.GTQ;
  case 'GYD': return currencyAmount * conversion.GYD;
  case 'HKD': return currencyAmount * conversion.HKD;
  case 'HNL': return currencyAmount * conversion.HNL;
  case 'HRK': return currencyAmount * conversion.HRK;
  case 'HTG': return currencyAmount * conversion.HTG;
  case 'HUF': return currencyAmount * conversion.HUF;
  case 'IDR': return currencyAmount * conversion.IDR;
  case 'ILS': return currencyAmount * conversion.ILS;
  case 'IMP': return currencyAmount * conversion.IMP;
  case 'INR': return currencyAmount * conversion.INR;
  case 'IQD': return currencyAmount * conversion.IQD;
  case 'IRR': return currencyAmount * conversion.IRR;
  case 'ISK': return currencyAmount * conversion.ISK;
  case 'JEP': return currencyAmount * conversion.JEP;
  case 'JMD': return currencyAmount * conversion.JMD;
  case 'JOD': return currencyAmount * conversion.JOD;
  case 'JPY': return currencyAmount * conversion.JPY;
  case 'KES': return currencyAmount * conversion.KES;
  case 'KGS': return currencyAmount * conversion.KGS;
  case 'KHR': return currencyAmount * conversion.KHR;
  case 'KID': return currencyAmount * conversion.KID;
  case 'KRW': return currencyAmount * conversion.KRW;
  case 'KWD': return currencyAmount * conversion.KWD;
  case 'KYD': return currencyAmount * conversion.KYD;
  case 'KZT': return currencyAmount * conversion.KZT;
  case 'LAK': return currencyAmount * conversion.LAK;
  case 'LBP': return currencyAmount * conversion.LBP;
  case 'LKR': return currencyAmount * conversion.LKR;
  case 'LRD': return currencyAmount * conversion.LRD;
  case 'LSL': return currencyAmount * conversion.LSL;
  case 'LYD': return currencyAmount * conversion.LYD;
  case 'MAD': return currencyAmount * conversion.MAD;
  case 'MDL': return currencyAmount * conversion.MDL;
  case 'MGA': return currencyAmount * conversion.MGA;
  case 'MKD': return currencyAmount * conversion.MKD;
  case 'MMK': return currencyAmount * conversion.MMK;
  case 'MNT': return currencyAmount * conversion.MNT;
  case 'MOP': return currencyAmount * conversion.MOP;
  case 'MRU': return currencyAmount * conversion.MRU;
  case 'MUR': return currencyAmount * conversion.MUR;
  case 'MVR': return currencyAmount * conversion.MVR;
  case 'MWK': return currencyAmount * conversion.MWK;
  case 'MXN': return currencyAmount * conversion.MXN;
  case 'MYR': return currencyAmount * conversion.MYR;
  case 'MZN': return currencyAmount * conversion.MZN;
  case 'NAD': return currencyAmount * conversion.NAD;
  case 'NGN': return currencyAmount * conversion.NGN;
  case 'NIO': return currencyAmount * conversion.NIO;
  case 'NOK': return currencyAmount * conversion.NOK;
  case 'NPR': return currencyAmount * conversion.NPR;
  case 'NZD': return currencyAmount * conversion.NZD;
  case 'OMR': return currencyAmount * conversion.OMR;
  case 'PAB': return currencyAmount * conversion.PAB;
  case 'PEN': return currencyAmount * conversion.PEN;
  case 'PGK': return currencyAmount * conversion.PGK;
  case 'PHP': return currencyAmount * conversion.PHP;
  case 'PKR': return currencyAmount * conversion.PKR;
  case 'PLN': return currencyAmount * conversion.PLN;
  case 'PYG': return currencyAmount * conversion.PYG;
  case 'QAR': return currencyAmount * conversion.QAR;
  case 'RON': return currencyAmount * conversion.RON;
  case 'RSD': return currencyAmount * conversion.RSD;
  case 'RUB': return currencyAmount * conversion.RUB;
  case 'RWF': return currencyAmount * conversion.RWF;
  case 'SAR': return currencyAmount * conversion.SAR;
  case 'SBD': return currencyAmount * conversion.SBD;
  case 'SCR': return currencyAmount * conversion.SCR;
  case 'SDG': return currencyAmount * conversion.SDG;
  case 'SEK': return currencyAmount * conversion.SEK;
  case 'SGD': return currencyAmount * conversion.SGD;
  case 'SHP': return currencyAmount * conversion.SHP;
  case 'SLL': return currencyAmount * conversion.SLL;
  case 'SOS': return currencyAmount * conversion.SOS;
  case 'SRD': return currencyAmount * conversion.SRD;
  case 'SSP': return currencyAmount * conversion.SSP;
  case 'STN': return currencyAmount * conversion.STN;
  case 'SVC': return currencyAmount * conversion.SVC;
  case 'SYP': return currencyAmount * conversion.SYP;
  case 'SZL': return currencyAmount * conversion.SZL;
  case 'THB': return currencyAmount * conversion.THB;
  case 'TJS': return currencyAmount * conversion.TJS;
  case 'TMT': return currencyAmount * conversion.TMT;
  case 'TND': return currencyAmount * conversion.TND;
  case 'TOP': return currencyAmount * conversion.TOP;
  case 'TRY': return currencyAmount * conversion.TRY;
  case 'TTD': return currencyAmount * conversion.TTD;
  case 'TVD': return currencyAmount * conversion.TVD;
  case 'TWD': return currencyAmount * conversion.TWD;
  case 'TZS': return currencyAmount * conversion.TZS;
  case 'UAH': return currencyAmount * conversion.UAH;
  case 'UGX': return currencyAmount * conversion.UGX;
  case 'USD': return currencyAmount * conversion.USD;
  case 'UYU': return currencyAmount * conversion.UYU;
  case 'UZS': return currencyAmount * conversion.UZS;
  case 'VES': return currencyAmount * conversion.VES;
  case 'VND': return currencyAmount * conversion.VND;
  case 'VUV': return currencyAmount * conversion.VUV;
  case 'WST': return currencyAmount * conversion.WST;
  case 'XAF': return currencyAmount * conversion.XAF;
  case 'XCD': return currencyAmount * conversion.XCD;
  case 'XDR': return currencyAmount * conversion.XDR;
  case 'XOF': return currencyAmount * conversion.XOF;
  case 'XPF': return currencyAmount * conversion.XPF;
  case 'YER': return currencyAmount * conversion.YER;
  case 'ZAR': return currencyAmount * conversion.ZAR;
  case 'ZMW': return currencyAmount * conversion.ZMW;
  case 'ZWL': return currencyAmount * conversion.ZWL;
  default:  
    console.error('Unsupported currency');
    return null;
  }
}